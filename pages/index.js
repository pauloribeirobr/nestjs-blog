import Head from 'next/head';
import Layout, { siteTitle } from '../components/layout';
import utilStyles from '../styles/utils.module.css';
import Link from 'next/link';
import Date from '../components/date';

export default function Home({allPostsData}) {
  return (
    <Layout home>
      <Head>
        <title>{siteTitle}</title>
      </Head>
      <section className={utilStyles.headingMd}>
        <p>Olá, eu sou o Paulo Ribeiro. Sou natural de Santo André / SP mas atualmente moro na cidade de São Paulo</p>
        <p>Sou programador Java, tendo trabalhado com PHP. Fui "Fullstack" quando o frontend era renderizado pelo backend (piada velha)</p>
        <p>Estou dando meus primeiros passos com o NextJs e em paralelo com React. Já trabalho com Angular</p>
        <p>
          (Este é um site de exemplo. Aprenda a montar o seu no{' '}
          <a href="https://nextjs.org/learn"> tutorial de Next.js</a>.)
        </p>
      </section>
      <section className={`${utilStyles.headingMd} ${utilStyles.padding1px}`}>
        <h2 className={utilStyles.headingLg}>Blog</h2>
        <ul className={utilStyles.list}>
          {allPostsData.map(({ id, date, title }) => (
            <li className={utilStyles.listItem} key={id}>
              <Link href={`/posts/${id}`}>
                <a>{title}</a>
              </Link>
              <br />
              <small className={utilStyles.lightText}>
                <Date dateString={date} />
              </small>
            </li>
          ))}
        </ul>
      </section>
    </Layout>
  );
}

import { getSortedPostsData } from '../lib/posts';

export async function getStaticProps() {
  const allPostsData = getSortedPostsData();
  return {
    props: {
      allPostsData,
    },
  }
}